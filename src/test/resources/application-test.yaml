spring:
  datasource:
    url: jdbc:mysql://localhost/test
    username: sa
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: false
logging:
  level:
    org.springframework: WARN
    com.zozo.realshop.query.stock.infrastructure.RealshopsMapper: DEBUG
